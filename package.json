{
  "name": "crunchy-paps",
  "private": true,
  "description": "Crunchy Paps — app web. Solo hay dependencias para las funciones de /api.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validar": "node tools/validar.mjs index.html && vite build"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "acorn": "^8.18.0",
    "acorn-walk": "^8.3.5",
    "vite": "^6.4.3"
  }
}
